# @package _global_
data_loader:
  _target_: srcs.data_loader.data_loaders.get_train_dataloader
  images_dir: data/train/ml_ozon_сounterfeit_train_images/
  tokenizer: ${tokenizer}
  tabular_cols: ${data.tabular_cols}
  target_col: ${data.target_col}
  id_col: ${data.id_col}
  batch_size: ${batch_size}
  num_workers: ${n_cpu}
